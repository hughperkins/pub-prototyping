cmake_minimum_required(VERSION 1.8.5)

Include(CheckFunctionExists)
check_function_exists( time HAVE_TIME)
message("Have time: ${HAVE_TIME}")

message("${HOME}")
message("~")
message("$ENV{HOME}")
#set(CMAKE_REQUIRED_LIBRARIES -L$ENV{HOME}/install/OpenBLAS-fromgit -lopenblas -lm -lgfortran -lpthread -lgomp -lrt )
message("MATH_LIBS: ${MATH_LIBS}")
#set(CMAKE_REQUIRED_LIBRARIES -L$ENV{HOME}/install/OpenBLAS-fromgit -lopenblas )
set(CMAKE_REQUIRED_LIBRARIES "${MATH_LIBS}")
#link_directories($ENV{HOME}/install/OpenBLAS-fromgit)
#link_libraries(openblas)
check_function_exists( daxpy HAVE_DAXPY )
check_function_exists( daxpy_ HAVE_DAXPY_POST )
message("HAVE_DAXPY: ${HAVE_DAXPY}")
message("HAVE_DAXPY_POST: ${HAVE_DAXPY_POST}")
if(HAVE_DAXPY )
   message("yes daxpy")
else(HAVE_DAXPY )
   message("no daxpy")
endif(HAVE_DAXPY)

